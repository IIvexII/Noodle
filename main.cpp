#include "lib/core/student.h"

int main(){
    StudentManagement student;

    // student.newRegistration();
    student.listAll();
    // student.rusticate();
    student.updateMarks(22, 99);
    return 0;
}
